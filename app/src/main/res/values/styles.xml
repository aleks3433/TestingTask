<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="labelsTextView" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:typeface">sans</item>
        <item name="android:textColor">@color/labelsTextViewColor</item>
        <item name="android:textSize">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingBottom">@dimen/allStylesPadding</item>
        <item name="android:paddingTop">@dimen/allStylesPadding</item>


    </style>

    <style name="descriptionsTextView" parent="@style/labelsTextView">
        <item name="android:textColor">@color/descriptionsTextViewColor</item>

    </style>

    <style name="statusTextView" parent="@style/labelsTextView">

        <item name="android:textColor">@color/statusTextViewColor</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/status</item>

    </style>

    <style name="dividers">
        <item name="android:background">@android:color/darker_gray</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dividersHeight</item>
    </style>


</resources>
